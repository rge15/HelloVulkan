#include <App/BaseApplication.hpp>
#include <utilities/graphicInclude.hpp>

struct RenderPassApp : BaseApplication
{
    RenderPassApp() = default;
    ~RenderPassApp() = default;
    
    void run() override;
};
