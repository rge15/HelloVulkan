#include <App/BaseApplication.hpp>
#include <utilities/graphicInclude.hpp>

struct InstanceExtensionLayersApp : BaseApplication
{
    InstanceExtensionLayersApp() = default;
    ~InstanceExtensionLayersApp() = default;
    void run() override;
};