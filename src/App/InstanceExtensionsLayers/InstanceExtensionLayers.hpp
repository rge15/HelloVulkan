#include <App/BaseApplication.hpp>
#include <utilities/graphicInclude.hpp>

struct InstanceExtensionLayers : BaseApplication
{
    InstanceExtensionLayers() = default;
    ~InstanceExtensionLayers() = default;
    void run() override;
};