#include <App/BaseApplication.hpp>
#include <utilities/graphicInclude.hpp>

struct SwapchainApp : BaseApplication
{
    SwapchainApp() = default;
    ~SwapchainApp() = default;
    void run() override;
};
