#include <App/BaseApplication.hpp>
#include <utilities/graphicInclude.hpp>

struct CommandBuffersApp : BaseApplication
{
    CommandBuffersApp() = default;
    ~CommandBuffersApp() = default;
    
    void run() override;
};
